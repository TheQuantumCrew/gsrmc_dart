name: sheet_remote_config
description: Google Sheet Remote Config written in Dart
version: 1.0.0
repository: https://github.com/TheQuantumCrew/sheet_remote_config

environment:
  sdk: ">=3.0.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  http: ^1.2.1
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
